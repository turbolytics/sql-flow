*.egg-info/
__pycache__/
.pytest_cache/
# `uv sync` builds this from uv.lock, which is committed.
.venv/
dev/volume/
bin/
dist/
.claude/

# duckdb/ducklake artifacts written by tests and examples
*.ducklake
*.ducklake.wal
*.ducklake.files/

# Test output the coverage matrix is generated from.
.coverage/

# Skills are repo tooling; worktrees and session state stay ignored.
!.claude/
.claude/*
!.claude/skills/
