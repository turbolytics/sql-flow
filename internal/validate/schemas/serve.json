{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://turbolytics.io/schemas/serve.json",
  "properties": {
    "commands": {
      "items": {
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of the command for reference."
          },
          "sql": {
            "type": "string",
            "description": "SQL statements to execute."
          }
        },
        "additionalProperties": false,
        "type": "object",
        "required": [
          "name",
          "sql"
        ],
        "description": "SQLCommand is one statement run at startup, before any table is created."
      },
      "type": "array",
      "description": "SQL commands run once at startup, before the server listens. Attach the\nbackend here, READ_ONLY."
    },
    "serve": {
      "properties": {
        "name": {
          "type": "string",
          "description": "Names this server in its logs."
        },
        "http": {
          "properties": {
            "addr": {
              "type": "string",
              "description": "host:port to listen on. Defaults to 0.0.0.0:8080."
            },
            "cors": {
              "properties": {
                "allowed_origins": {
                  "items": {
                    "type": "string"
                  },
                  "type": "array",
                  "description": "Exact origins, such as https://turbolytics.io: a scheme, a host and an\noptional port. No path and no wildcard."
                }
              },
              "additionalProperties": false,
              "type": "object",
              "required": [
                "allowed_origins"
              ],
              "description": "Browser origins allowed to call. Omit to send no CORS headers."
            }
          },
          "additionalProperties": false,
          "type": "object",
          "description": "Where to listen, and which browser origins may call."
        },
        "auth": {
          "properties": {
            "tokens": {
              "items": {
                "properties": {
                  "name": {
                    "type": "string",
                    "description": "Names the client in the request log."
                  },
                  "token": {
                    "type": "string",
                    "description": "The value a client sends as Authorization: Bearer \u003ctoken\u003e. It is an\nidentifier, not a secret: a browser page ships it in plain sight."
                  }
                },
                "additionalProperties": false,
                "type": "object",
                "required": [
                  "name",
                  "token"
                ],
                "description": "ServeToken names one client identity."
              },
              "type": "array",
              "description": "One entry per client identity."
            }
          },
          "additionalProperties": false,
          "type": "object",
          "required": [
            "tokens"
          ],
          "description": "Who may call."
        },
        "limits": {
          "properties": {
            "max_rows": {
              "type": "integer",
              "description": "Most rows one response carries. 0 means 10000. A cut result says\ntruncated: true."
            },
            "timeout_seconds": {
              "type": "integer",
              "description": "Longest a caller waits for a query, in seconds. 0 means 10. The query\nitself runs to completion: DuckDB cannot be cancelled."
            },
            "rate_limit": {
              "properties": {
                "requests_per_second": {
                  "type": "number",
                  "description": "Reserved. Must be 0."
                },
                "burst": {
                  "type": "integer",
                  "description": "Reserved. Must be 0."
                }
              },
              "additionalProperties": false,
              "type": "object",
              "description": "Reserved. Parsed so a config written now keeps its shape; refused when\nany field is non-zero."
            }
          },
          "additionalProperties": false,
          "type": "object",
          "description": "Limits for every dataset. A dataset's own non-zero limit overrides one."
        },
        "pool": {
          "properties": {
            "size": {
              "type": "integer",
              "description": "Sessions the server holds. Each is one backend session, and one\nrequest uses one at a time, so this is the requests that run at once.\n0 means the default, 4."
            }
          },
          "additionalProperties": false,
          "type": "object",
          "description": "How many requests the server runs at once. Omit for the default."
        },
        "metrics": {
          "properties": {
            "enabled": {
              "type": "boolean",
              "description": "Serve GET /metrics on the same listener as the datasets, without a\ntoken. Off by default: that listener is public, and the metric labels\nname every dataset and grain."
            }
          },
          "additionalProperties": false,
          "type": "object",
          "description": "Whether to serve Prometheus metrics at /metrics."
        },
        "cache": {
          "properties": {
            "max_mb": {
              "type": "integer",
              "description": "The most the cache holds, in MiB. 0 means 16."
            }
          },
          "additionalProperties": false,
          "type": "object",
          "description": "Bounds the response cache. It does not enable it: a dataset opts in\nwith its own cache block."
        },
        "datasets": {
          "items": {
            "properties": {
              "name": {
                "type": "string",
                "description": "The URL path segment: /v1/datasets/\u003cname\u003e. Lowercase letters, digits\nand underscores, starting with a letter."
              },
              "description": {
                "type": "string",
                "description": "Shown in /v1/datasets."
              },
              "params": {
                "items": {
                  "properties": {
                    "name": {
                      "type": "string",
                      "description": "The query parameter name, and the $name the SQL reads."
                    },
                    "type": {
                      "type": "string",
                      "enum": [
                        "string",
                        "integer",
                        "timestamp"
                      ],
                      "description": "How the value parses and binds: string as VARCHAR, integer as BIGINT,\ntimestamp (RFC 3339 with an offset) as TIMESTAMP WITH TIME ZONE."
                    },
                    "min": {
                      "type": "integer",
                      "description": "The smallest value an integer param accepts. A request below it is\nrefused with 400 invalid_param, never clamped: a clamp answers with less\nthan the caller asked for and does not say so."
                    },
                    "max": {
                      "type": "integer",
                      "description": "The largest value an integer param accepts, refused the same way."
                    }
                  },
                  "additionalProperties": false,
                  "type": "object",
                  "required": [
                    "name",
                    "type"
                  ],
                  "description": "ServeParam declares one query parameter."
                },
                "type": "array",
                "description": "Query parameters the SQL reads as $name. Every statement must use every\nparam. An absent param binds NULL; default it in SQL with coalesce."
              },
              "limits": {
                "properties": {
                  "max_rows": {
                    "type": "integer",
                    "description": "Most rows one response carries. 0 means 10000. A cut result says\ntruncated: true."
                  },
                  "timeout_seconds": {
                    "type": "integer",
                    "description": "Longest a caller waits for a query, in seconds. 0 means 10. The query\nitself runs to completion: DuckDB cannot be cancelled."
                  },
                  "rate_limit": {
                    "properties": {
                      "requests_per_second": {
                        "type": "number",
                        "description": "Reserved. Must be 0."
                      },
                      "burst": {
                        "type": "integer",
                        "description": "Reserved. Must be 0."
                      }
                    },
                    "additionalProperties": false,
                    "type": "object",
                    "description": "Reserved. Parsed so a config written now keeps its shape; refused when\nany field is non-zero."
                  }
                },
                "additionalProperties": false,
                "type": "object",
                "description": "Overrides the top-level limits for this dataset."
              },
              "cache": {
                "properties": {
                  "ttl_seconds": {
                    "type": "integer",
                    "minimum": 1,
                    "description": "How long an answer is served after the query that produced it\nstarted, in seconds. At least 1."
                  }
                },
                "additionalProperties": false,
                "type": "object",
                "required": [
                  "ttl_seconds"
                ],
                "description": "Opts the dataset into the response cache. Absent, every request runs\nits query."
              },
              "sql": {
                "type": "string",
                "description": "The statement, for a dataset without grains. Set sql or grains."
              },
              "grains": {
                "additionalProperties": {
                  "properties": {
                    "bucket": {
                      "type": "string",
                      "description": "How wide one bucket of this grain is, such as 5m. Required on every\ngrain of a cached dataset with a range, and refused without a range.\nIt must divide one day. Units: s, m, h, d."
                    },
                    "max_range": {
                      "type": "string",
                      "description": "The widest range this grain serves, such as 14d. Required when the\ndataset declares a range, and refused otherwise. Units: s, m, h, d."
                    },
                    "sql": {
                      "type": "string",
                      "description": "The statement for this grain."
                    }
                  },
                  "additionalProperties": false,
                  "type": "object",
                  "required": [
                    "sql"
                  ],
                  "description": "ServeGrain is one grain's statement."
                },
                "type": "object",
                "description": "One statement per grain, selected by ?grain=\u003cname\u003e. Set sql or grains."
              },
              "range": {
                "properties": {
                  "since": {
                    "type": "string",
                    "description": "The timestamp param that starts the range. Absent from a request, it\nbinds until minus default."
                  },
                  "until": {
                    "type": "string",
                    "description": "The timestamp param that ends the range. Absent from a request, it\nbinds the time the request arrived."
                  },
                  "default": {
                    "type": "string",
                    "description": "The width of a range a request does not bound with since, such as 24h.\nUnits: s, m, h, d."
                  }
                },
                "additionalProperties": false,
                "type": "object",
                "required": [
                  "since",
                  "until",
                  "default"
                ],
                "description": "Names the params that bound a time range. With a range, a request may\nomit grain: the server picks the finest grain whose max_range covers\nthe requested range. Requires grains."
              }
            },
            "additionalProperties": false,
            "type": "object",
            "required": [
              "name"
            ],
            "description": "ServeDataset is one named, parameterized SQL statement, or one per grain."
          },
          "type": "array",
          "description": "The datasets this server answers. Nothing else is reachable."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "auth",
        "datasets"
      ],
      "description": "What to serve, to whom, and within which limits."
    }
  },
  "additionalProperties": false,
  "type": "object",
  "required": [
    "serve"
  ],
  "description": "ServeConf is a whole serve file: the commands that attach the data, and the serve block that exposes it."
}
